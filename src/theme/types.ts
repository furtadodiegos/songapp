export interface AppPaletteProps {
  primary: string;
  primaryDark: string;
  primaryLight: string;
  secondary: string;
  primaryText: string;
  secondaryText: string;
  captionText: string;
  divider: string;
  gradient: string;
}
